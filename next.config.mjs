/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['c14.patreon.com', 'lh3.googleusercontent.com', 'avatars.githubusercontent.com']
    },
};

export default nextConfig;
